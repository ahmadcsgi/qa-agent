# QA Agent memory — personal learning, do not commit
.cursor/qa-memory/

# private onboard (share offline only — do not push)
onboard.md

# Cursor plugin cache (noisy machine-local; never relevant to QA Agent)
.cursor/plugins/

# OpenCode runtime state — machine-specific, do not commit
.omo/

# Visual test baselines (auto-generated reference screenshots)
.cursor/skills/qa-visual-test/baselines/

# Visual test npm dependencies
.cursor/skills/qa-visual-test/scripts/node_modules/
